# 🚀 Quick Start Guide / คู่มือเริ่มต้นอย่างรวดเร็ว

## เริ่มใช้งานใน 5 นาที / Get Started in 5 Minutes

### ขั้นตอนที่ 1: ติดตั้ง Dependencies

```bash
# เข้าไปที่โฟลเดอร์ server
cd server
npm install

# เข้าไปที่โฟลเดอร์ client
cd ../client
npm install
```

### ขั้นตอนที่ 2: เริ่มต้น Server

```bash
# ใน terminal หนึ่ง
cd server
npm start
```

### ขั้นตอนที่ 3: เริ่มต้น Client

```bash
# เปิด terminal ใหม่
cd client
npm run dev
```

### ขั้นตอนที่ 4: เปิดเบราว์เซอร์

เข้าไปที่: `http://localhost:3000`

---

## 🎯 ทดสอบความสามารถ / Try Out Features

### 1. สร้าง Task
- คลิกที่แท็บ "📋 งาน / Tasks"
- กรอกชื่องาน เช่น "ทำการบ้าน"
- เลือกระดับความสำคัญ
- คลิก "สร้างงาน"

### 2. สร้าง Note
- คลิกที่แท็บ "📝 โน้ต / Notes"
- กรอกหัวข้อและเนื้อหา
- เพิ่มแท็ก เช่น "สำคัญ, ทำงาน"
- คลิก "สร้างโน้ต"

### 3. ตรวจสอบสภาพอากาศ
- คลิกที่แท็บ "🌤️ สภาพอากาศ / Weather"
- กรอกชื่อเมือง เช่น "Bangkok"
- คลิก "ตรวจสอบ"

---

## 🔌 การเชื่อมต่อกับ Claude Desktop

### สำหรับ macOS

1. สร้าง/แก้ไขไฟล์:
```bash
~/Library/Application Support/Claude/claude_desktop_config.json
```

2. เพิ่ม configuration:
```json
{
  "mcpServers": {
    "react-mcp-example": {
      "command": "node",
      "args": [
        "/Users/YOUR_USERNAME/path/to/react-mcp-example/server/index.js"
      ]
    }
  }
}
```

3. รีสตาร์ท Claude Desktop

### สำหรับ Windows

1. สร้าง/แก้ไขไฟล์:
```
%APPDATA%\Claude\claude_desktop_config.json
```

2. เพิ่ม configuration:
```json
{
  "mcpServers": {
    "react-mcp-example": {
      "command": "node",
      "args": [
        "C:\\path\\to\\react-mcp-example\\server\\index.js"
      ]
    }
  }
}
```

3. รีสตาร์ท Claude Desktop

### ตัวอย่างการใช้งานกับ Claude

ลองถามคำถามเหล่านี้กับ Claude:

```
1. "สร้างงานใหม่ชื่อ 'ทำรายงาน' ความสำคัญสูง"

2. "แสดงรายการงานทั้งหมด"

3. "สร้างโน้ตเกี่ยวกับการประชุมวันนี้"

4. "ค้นหาโน้ตที่มีคำว่า 'project'"

5. "สภาพอากาศในกรุงเทพวันนี้เป็นยังไง?"
```

---

## 🛠️ Development Mode

### เปิด Auto-reload สำหรับ Server

```bash
cd server
npm run dev
```

### เปิด Development Tools

```bash
# MCP Inspector สำหรับ debug server
cd server
npm run inspector

# React Dev Tools จะทำงานอัตโนมัติใน browser
```

---

## 📱 Responsive Design

Client รองรับการแสดงผลบนหลายอุปกรณ์:
- 💻 Desktop
- 📱 Mobile
- 📲 Tablet

---

## 🎨 Customization

### แก้สี Theme

แก้ไขไฟล์ `client/src/index.css`:

```css
:root {
  --primary-color: #646cff;  /* เปลี่ยนสีหลัก */
  --background: #242424;      /* เปลี่ยนสีพื้นหลัง */
}
```

### เพิ่ม Tool ใหม่

แก้ไขไฟล์ `server/index.js` ตามคู่มือใน [server/README.md](server/README.md)

---

## ⚡ Tips & Tricks

### 1. ใช้ Keyboard Shortcuts
- `Ctrl/Cmd + K` เพื่อ focus search
- `Tab` เพื่อเปลี่ยนระหว่างฟิลด์

### 2. Performance
- Client จะ cache ผลลัพธ์เพื่อความเร็ว
- Server ใช้ in-memory storage (เหมาะสำหรับ demo)

### 3. Debugging
- เปิด Browser Console (F12) เพื่อดู errors
- ใช้ MCP Inspector เพื่อ debug server

---

## 📚 เอกสารเพิ่มเติม / Additional Documentation

- [README.md](README.md) - เอกสารหลัก
- [server/README.md](server/README.md) - รายละเอียด Server
- [MCP Docs](https://modelcontextprotocol.io) - เอกสาร MCP

---

## ❓ คำถามที่พบบ่อย / FAQ

### Q: ใช้กับ AI models อื่นได้ไหม?
A: ได้ครับ! MCP รองรับ Claude, GPT, และ AI models อื่นๆ ที่รองรับ MCP protocol

### Q: ข้อมูลจะหายเมื่อรีสตาร์ท server ไหม?
A: ใช่ครับ เพราะใช้ in-memory storage ในการใช้งานจริงควรใช้ database

### Q: สามารถใช้ใน production ได้ไหม?
A: ได้ครับ แต่ควรเพิ่ม:
  - Database แทน in-memory storage
  - Authentication
  - Rate limiting
  - Error handling ที่ดีขึ้น
  - Monitoring & logging

### Q: รองรับภาษาไทยไหม?
A: ใช่ครับ! UI และเอกสารรองรับทั้งภาษาไทยและอังกฤษ

---

## 🆘 ต้องการความช่วยเหลือ / Need Help?

หากพบปัญหา:
1. ดูที่ [Troubleshooting](README.md#troubleshooting)
2. ตรวจสอบ Console logs
3. เปิด Issue ใน GitHub repository

---

<div align="center">
  <p>🎉 ขอให้สนุกกับการพัฒนา! / Happy coding! 🎉</p>
</div>
