# คู่มือการใช้งาน MCP Server (ภาษาไทย)

<div align="center">
  <h1>Model Context Protocol (MCP) Server</h1>
  <p>คู่มือฉบับภาษาไทยสำหรับการติดตั้งและใช้งาน MCP Server</p>
</div>

## 📋 สารบัญ
- [ภาพรวม](#ภาพรวม)
- [ข้อกำหนดเบื้องต้น](#ข้อกำหนดเบื้องต้น)
- [การติดตั้ง](#การติดตั้ง)
- [การตั้งค่า](#การตั้งค่า)
- [การใช้งานจากเครื่องท้องถิ่น](#การใช้งานจากเครื่องท้องถิ่น)
- [การเชื่อมต่อ](#การเชื่อมต่อ)
- [การทดสอบ](#การทดสอบ)
- [การรายงานผล](#การรายงานผล)
- [ตัวอย่างการใช้งาน](#ตัวอย่างการใช้งาน)
- [การแก้ปัญหา](#การแก้ปัญหา)

## 🔍 ภาพรวม

Model Context Protocol (MCP) เป็นโปรโตคอลมาตรฐานเปิดที่ช่วยให้แอปพลิเคชัน AI สามารถเชื่อมต่อกับแหล่งข้อมูลและเครื่องมือภายนอกได้

### MCP คืออะไร?
MCP เปรียบเสมือนพอร์ต USB-C สำหรับแอปพลิเคชัน AI โดยทำหน้าที่เป็นมาตรฐานในการเชื่อมต่อ AI models (เช่น Claude, ChatGPT) เข้ากับ:
- แหล่งข้อมูล (ไฟล์ในเครื่อง, ฐานข้อมูล)
- เครื่องมือต่างๆ (search engines, calculators)
- Workflows (prompts เฉพาะทาง)

### ความสามารถของ MCP
- ✅ เข้าถึง Google Calendar และ Notion เพื่อทำหน้าที่เป็น AI assistant ส่วนตัว
- ✅ สร้าง web app จาก Figma design
- ✅ เชื่อมต่อกับหลายฐานข้อมูลในองค์กร
- ✅ สร้างการออกแบบ 3D บน Blender

## 📦 ข้อกำหนดเบื้องต้น

### สิ่งที่จำเป็นต้องมี:

#### 1. ซอฟต์แวร์พื้นฐาน
- **Node.js**: เวอร์ชัน 16 หรือสูงกว่า
- **npm**: Package manager (มาพร้อม Node.js)
- **Git**: สำหรับจัดการ version control

#### 2. API Key
- **Moralis API Key**: จำเป็นสำหรับการเชื่อมต่อกับ Moralis APIs
  - สมัครที่: [Moralis Developer Portal](https://admin.moralis.com)
  - สร้าง API Key ที่: [API Keys Page](https://admin.moralis.com/api-keys)

#### 3. ข้อมูลเครื่อง
- **ระบบปฏิบัติการ**: Windows, macOS, หรือ Linux
- **RAM**: แนะนำอย่างน้อย 4GB
- **พื้นที่จัดเก็บ**: อย่างน้อย 500MB สำหรับ dependencies

## 🚀 การติดตั้ง

### วิธีที่ 1: ติดตั้งผ่าน npm (แนะนำ)

```bash
# ติดตั้ง MCP Server แบบ global
npm install -g @moralisweb3/api-mcp-server

# หรือติดตั้งในโปรเจค
npm install @moralisweb3/api-mcp-server
```

### วิธีที่ 2: ติดตั้งผ่าน Smithery (สำหรับ Claude Desktop)

```bash
# ติดตั้งอัตโนมัติสำหรับ Claude Desktop
npx -y @smithery/cli install @MoralisWeb3/moralis-mcp-server --client claude
```

### วิธีที่ 3: Clone Repository (สำหรับ Development)

```bash
# Clone repository
git clone https://github.com/gittisak-go/gittisak-go.git
cd gittisak-go

# ติดตั้ง dependencies
npm install

# Build project
npm run build
```

## ⚙️ การตั้งค่า

### 1. ตั้งค่า Environment Variables

#### บน macOS/Linux:
```bash
# เพิ่มใน ~/.bashrc หรือ ~/.zshrc
export MORALIS_API_KEY="your_api_key_here"

# หรือใช้แบบชั่วคราว
export MORALIS_API_KEY="your_api_key_here"
```

#### บน Windows (Command Prompt):
```cmd
set MORALIS_API_KEY=your_api_key_here
```

#### บน Windows (PowerShell):
```powershell
$env:MORALIS_API_KEY="your_api_key_here"
```

### 2. ตั้งค่าไฟล์ Configuration

สร้างไฟล์ `mcp.json` ในไดเรกทอรีที่ต้องการ:

```json
{
  "mcpServers": {
    "moralis": {
      "command": "npx",
      "args": ["@moralisweb3/api-mcp-server"],
      "env": {
        "MORALIS_API_KEY": "your_api_key_here"
      }
    }
  }
}
```

### 3. ตั้งค่าสำหรับ Claude Desktop

สำหรับผู้ใช้ Claude Desktop สร้างหรือแก้ไขไฟล์:

**macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`

**Windows**: `%APPDATA%\Claude\claude_desktop_config.json`

```json
{
  "mcpServers": {
    "moralis": {
      "command": "npx",
      "args": ["-y", "@moralisweb3/api-mcp-server"],
      "env": {
        "MORALIS_API_KEY": "your_api_key_here"
      }
    }
  }
}
```

## 💻 การใช้งานจากเครื่องท้องถิ่น

### เริ่มต้น MCP Server

MCP Server รองรับการเชื่อมต่อแบบต่างๆ ผ่าน parameter `--transport`:

#### 1. แบบ stdio (ค่าเริ่มต้น)
```bash
# เหมาะสำหรับการเชื่อมต่อกับ AI clients
moralis-api-mcp --transport stdio
```

#### 2. แบบ web
```bash
# เริ่ม HTTP server
moralis-api-mcp --transport web

# Server จะทำงานที่ port 3000 (ค่าเริ่มต้น)
# เข้าถึงได้ที่: http://localhost:3000
```

#### 3. แบบ streamable-http
```bash
# เริ่ม HTTP server พร้อม streaming endpoints
moralis-api-mcp --transport streamable-http
```

### ใช้งานผ่าน npx (ไม่ต้องติดตั้ง)

```bash
# รัน MCP Server โดยไม่ต้องติดตั้งก่อน
npx @moralisweb3/api-mcp-server --transport stdio
```

### Development Mode

```bash
# สำหรับการพัฒนาที่มี auto-rebuild
npm run watch

# ใช้ MCP Inspector สำหรับ debug
npm run inspector
```

## 🔌 การเชื่อมต่อ

### ข้อมูลที่จำเป็นสำหรับการเชื่อมต่อ:

#### 1. API Key (บังคับ)
- **Moralis API Key**: ใช้สำหรับการเข้าถึง Moralis APIs
- **วิธีการขอ API Key**:
  1. ไปที่ [Moralis Admin](https://admin.moralis.com)
  2. ลงทะเบียนและเข้าสู่ระบบ
  3. ไปที่เมนู API Keys
  4. คัดลอก API Key ของคุณ

#### 2. รหัสผ่านและการยืนยันตัวตน
- **ไม่จำเป็นต้องใช้รหัสผ่าน**: MCP Server ใช้ API Key เพียงอย่างเดียว
- **การรักษาความปลอดภัย**: เก็บ API Key ไว้เป็นความลับ อย่าแชร์หรือ commit ลง Git

#### 3. Network และ Port
- **Port เริ่มต้น**: 3000 (สำหรับ web transport)
- **Localhost**: การเชื่อมต่อผ่าน `http://localhost:3000`
- **Firewall**: ตรวจสอบว่า port ที่ใช้ไม่ถูกบล็อก

### ประเภทของการเชื่อมต่อที่รองรับ:

| ประเภท | คำอธิบาย | ใช้กับ |
|--------|----------|--------|
| stdio | Standard input/output | AI clients (Claude Desktop, VS Code) |
| web | HTTP server | Web applications, REST API calls |
| streamable-http | HTTP server with streaming | Real-time applications |

### ตรวจสอบการเชื่อมต่อ

```bash
# ทดสอบว่า API Key ใช้งานได้
curl -X GET "https://deep-index.moralis.io/api/v2.2/erc20/metadata" \
  -H "X-API-Key: your_api_key_here"

# ทดสอบ MCP Server (หากใช้ web transport)
curl http://localhost:3000/health
```

## 🧪 การทดสอบ

### การทดสอบการติดตั้ง

```bash
# ตรวจสอบว่า Node.js ติดตั้งแล้ว
node --version

# ตรวจสอบว่า npm ติดตั้งแล้ว
npm --version

# ตรวจสอบว่า MCP Server ติดตั้งแล้ว
npx @moralisweb3/api-mcp-server --help
```

### การทดสอบการเชื่อมต่อ

```bash
# ทดสอบ API Key
export MORALIS_API_KEY="your_api_key_here"
npx @moralisweb3/api-mcp-server --transport stdio
```

### การทดสอบผ่าน MCP Inspector

```bash
# เปิด MCP Inspector สำหรับ debug
npm run inspector

# Inspector จะให้ URL สำหรับเปิดใน browser
# เช่น: http://localhost:5173
```

### การทดสอบ API Endpoints

```bash
# ทดสอบการดึงข้อมูล token metadata
curl http://localhost:3000/api/token/metadata \
  -H "Content-Type: application/json" \
  -d '{"address": "0x....", "chain": "eth"}'
```

## 📊 การรายงานผล

### โครงสร้างการรายงาน

#### 1. Log Files
```bash
# ดู logs ของ MCP Server
tail -f ~/.mcp/logs/moralis-mcp-server.log

# หรือใช้ console output
moralis-api-mcp --transport stdio 2>&1 | tee server.log
```

#### 2. รูปแบบการรายงาน

**รายงานการติดตั้ง:**
```
✅ ติดตั้ง Node.js เวอร์ชัน: v18.16.0
✅ ติดตั้ง npm เวอร์ชัน: 9.5.1
✅ ติดตั้ง MCP Server สำเร็จ
✅ ตั้งค่า MORALIS_API_KEY สำเร็จ
```

**รายงานการเชื่อมต่อ:**
```
🔌 กำลังเชื่อมต่อ MCP Server...
✅ เชื่อมต่อ Moralis API สำเร็จ
✅ รับรองสิทธิ์ด้วย API Key สำเร็จ
📍 Server ทำงานที่: http://localhost:3000
⚡ Transport: web
```

**รายงานการทดสอบ:**
```
🧪 กำลังทดสอบ MCP Server...
✅ ทดสอบการเชื่อมต่อ: ผ่าน
✅ ทดสอบ API Key: ผ่าน
✅ ทดสอบ Endpoint: ผ่าน
📊 ผลการทดสอบ: 3/3 ผ่าน
```

#### 3. Metrics และ Performance

```bash
# ดู performance metrics
curl http://localhost:3000/metrics

# ตัวอย่าง output:
# - Request count: 150
# - Average response time: 45ms
# - Error rate: 0.5%
# - Uptime: 99.9%
```

### สรุปผลการใช้งาน

**รายงานประจำวัน:**
- จำนวน requests ทั้งหมด
- เวลาตอบสนองเฉลี่ย
- อัตราข้อผิดพลาด
- API endpoints ที่ใช้บ่อย

**รายงานความผิดพลาด:**
- Error codes และ messages
- Stack traces
- Timestamp ของข้อผิดพลาด
- แนวทางแก้ไข

## 📝 ตัวอย่างการใช้งาน

### ตัวอย่างที่ 1: ดึงข้อมูลราคา Token

```javascript
// ถาม AI ผ่าน MCP
"ราคาปัจจุบันของ PEPE และ Ethereum เท่าไหร่?"

// AI จะเรียก MCP Server และได้ผลลัพธ์:
// PEPE: $0.000012
// Ethereum: $2,345.67
```

### ตัวอย่างที่ 2: ตรวจสอบ Wallet

```javascript
// ถาม AI
"โทเคนอะไรบ้างที่ wallet 0xabc...123 ถืออยู่?"

// ผลลัพธ์:
// - USDT: 1,000 tokens
// - USDC: 500 tokens
// - DAI: 250 tokens
```

### ตัวอย่างที่ 3: วิเคราะห์ NFT

```javascript
// ถาม AI
"แสดง NFTs ที่ vitalik.eth เป็นเจ้าของบน Base"

// ผลลัพธ์แสดง:
// - NFT collection names
// - Token IDs
// - Metadata
```

### ตัวอย่างที่ 4: ประวัติธุรกรรม

```javascript
// ถาม AI
"แสดงประวัติธุรกรรมทั้งหมดของ wallet 0xabc...123"

// ผลลัพธ์:
// - Transaction hashes
// - Timestamps
// - Values
// - Gas fees
```

## 🔧 การแก้ปัญหา

### ปัญหาที่พบบ่อย

#### 1. API Key ไม่ถูกต้อง
```
❌ Error: Invalid API Key

✅ แก้ไข:
- ตรวจสอบว่า API Key ถูกต้อง
- ตรวจสอบว่าไม่มีช่องว่างหรืออักขระพิเศษ
- ตรวจสอบว่า API Key ยังไม่หมดอายุ
```

#### 2. ไม่สามารถเชื่อมต่อได้
```
❌ Error: Connection refused

✅ แก้ไข:
- ตรวจสอบว่า server กำลังทำงาน
- ตรวจสอบ port ที่ใช้
- ตรวจสอบ firewall settings
```

#### 3. ติดตั้ง dependencies ไม่สำเร็จ
```
❌ Error: npm install failed

✅ แก้ไข:
- ลบ node_modules และ package-lock.json
- รัน: npm cache clean --force
- รัน: npm install อีกครั้ง
```

#### 4. Permission Error
```
❌ Error: EACCES permission denied

✅ แก้ไข:
- ใช้ sudo (macOS/Linux): sudo npm install -g
- รัน terminal as administrator (Windows)
- ตรวจสอบ file permissions
```

### การขอความช่วยเหลือ

หากพบปัญหาที่แก้ไม่ได้:

1. **ตรวจสอบ Documentation**:
   - [Moralis API Docs](https://deep-index.moralis.io/api-docs-2.2/)
   - [MCP Documentation](https://modelcontextprotocol.io)

2. **ติดต่อ Support**:
   - GitHub Issues: [gittisak-go/gittisak-go](https://github.com/gittisak-go/gittisak-go/issues)
   - Moralis Support: [Moralis Support Portal](https://support.moralis.com)

3. **Community**:
   - Discord: Moralis Discord Server
   - Forum: Moralis Community Forum

## 📍 ข้อมูลที่ตั้งและ Resources

### Repository Location
- **GitHub**: https://github.com/gittisak-go/gittisak-go
- **Documentation**: https://gittisak-go.github.io/gittisak-go

### Local Installation Paths

#### macOS/Linux:
```
- Global npm packages: /usr/local/lib/node_modules
- Claude Desktop config: ~/Library/Application Support/Claude
- MCP logs: ~/.mcp/logs
```

#### Windows:
```
- Global npm packages: %APPDATA%\npm\node_modules
- Claude Desktop config: %APPDATA%\Claude
- MCP logs: %USERPROFILE%\.mcp\logs
```

### API Endpoints
- **Moralis API**: https://deep-index.moralis.io/api/v2.2
- **MCP Server** (local): http://localhost:3000

## 📚 เอกสารเพิ่มเติม

### ภาษาอังกฤษ
- [README.md](./README.md) - MCP Protocol Overview
- [README1.md](./README1.md) - Moralis MCP Server Documentation
- [README-2.md](./README-2.md) - Python Integration Example

### External Resources
- [MCP Protocol Specification](https://modelcontextprotocol.io)
- [Moralis API Documentation](https://docs.moralis.io)
- [Claude Desktop Guide](https://support.claude.com)

## 📄 สิทธิ์การใช้งาน

โปรเจคนี้เผยแพร่ภายใต้ [MIT License](./LICENSE)

---

## ✨ สรุป

เอกสารนี้ครอบคลุม:
- ✅ **การทดสอบและรายงานผล**: วิธีทดสอบและดูผลการทำงาน
- ✅ **ข้อมูลที่ตั้ง**: ตำแหน่งไฟล์และ resources ต่างๆ
- ✅ **คู่มือการใช้งานท้องถิ่น**: วิธีรัน MCP Server บนเครื่องของคุณ
- ✅ **ข้อมูลการเชื่อมต่อ**: วิธีเชื่อมต่อกับ MCP Server
- ✅ **ข้อกำหนด**: API Keys และสิ่งที่จำเป็น

หากมีคำถามเพิ่มเติม กรุณาตรวจสอบเอกสารหรือติดต่อผ่าน GitHub Issues

---

**อัพเดทล่าสุด**: พฤศจิกายน 2025
